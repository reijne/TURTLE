#include "../../../config.h"

using namespace std;

#include "VarSizeReadOnlyCache.h"

#include <iostream>

#include "TableKey.h"


int main()
{
/*VarSizeReadOnlyCache<TableKey, RepresentationMatrices>	cache(24, 10000);
Configuration<MOType>	conf1(2, 0, 1, 2);
Configuration<MOType>	conf2(0, 1, 1);
BinomialCoefficient	binom(MAXOPENSHELLS);
RepresentationMatrices::spinEigs = new
	SpinEigenFunctionDegeneration(2, MAXOPENSHELLS);
	
	cout << *RepresentationMatrices::spinEigs << endl;

TableCase<MOType>	tc(&binom);
	tc.calcLess3(conf1, conf2);

	for ( INT i=0 ; i<2000 ; i++ )
		for ( INT j=1 ; j<26 ; j++ )
		{	tc = TableCase<MOType>(i & 7, j);
			cache[TableKey(tc)];
			cache[TableKey(tc)];
			cache[TableKey(tc)];
			cache[TableKey(tc)];
		}
	
*/
TableKey	tk;
	cout << tk << endl;

//	cout << cache << endl;


}
