c
      REAL eps, epss, epsana, epstab, sytol, tolmy, fetda, ferpa
      REAL tottda,totrpa
      integer modus, nevlo, nevhi, nev, maxr, maxit, maxrr, maxitt
      integer  nbegin, ipref,iblo13
      common /rpacom/ modus,nevlo(8),nevhi(8),nev(8),
     +                maxr,maxit,maxrr,maxitt,nbegin,
     +                eps,epss,epsana,epstab,
     +                sytol,tolmy,fetda,ferpa,tottda,totrpa,ipref,iblo13
c
