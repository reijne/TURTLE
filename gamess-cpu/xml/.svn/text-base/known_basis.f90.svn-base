
module known_basis

  use xml_modules

  integer num_atomic_basis_sets
  ! 50 basis sets of 50 groups ?
  character(len=30)  basis_ids(50,50)
  integer len_atomic_basis_set(50)  ! length in terms of groups/shells
  integer atom_basis(maxat)

end module known_basis
