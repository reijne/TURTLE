dnl this is the machine dependent file for the rs6000
define(GEN_OPTIONS,`rs6000,cio')dnl
define(GEN_MACHINE,`r')dnl
define(REAL,real*8)dnl
define(COMPLEX,complex*16)dnl
changequote(!,!)dnl
define(INCLUDE,_INCLUDE($1))dnl
changequote()dnl
define(dfloat,dble($1))dnl
define(leadz,leadzz($1))dnl
