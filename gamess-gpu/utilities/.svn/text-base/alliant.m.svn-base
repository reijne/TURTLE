dnl this is the machine dependent file for the alliant fx2808
define(GEN_OPTIONS,`alliant,fx2808,cio,vector')dnl
define(GEN_MACHINE,`a')dnl
define(REAL,real*8)dnl
define(COMPLEX,complex*16)dnl
changequote(!,!)dnl
define(INCLUDE,!      include '$1'!)dnl
changequote()dnl
