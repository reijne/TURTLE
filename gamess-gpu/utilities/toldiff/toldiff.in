#!/bin/sh
#
# Tell toldiff to use an external diff program for performance
#
export TOLDIFF_EXE=`which diff`
#
# Invoke toldiff
#
# This script is processed by the configure script to set ToLDIFFDIR
# to point to this directory
# 
python TOLDIFFDIR/toldiff.py $1 $2 $3 $4 $5 $6 $7 $8 $9
